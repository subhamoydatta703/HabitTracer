# Copy this file to `.env` and fill in the real values.
# DATABASE_URL is used by Prisma (see prisma.config.ts).
# It is a PostgreSQL connection string, e.g.:
#   postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public

DATABASE_URL=